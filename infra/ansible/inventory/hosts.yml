all:
  children:
    homelab:
      hosts:
        example-host:
          ansible_host: your-homelab-hostname-or-ip
          ansible_user: your-user
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          ansible_python_interpreter: /usr/bin/python3
